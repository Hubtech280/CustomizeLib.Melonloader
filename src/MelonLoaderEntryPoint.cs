namespace CustomizeLib.MelonLoader;

/// <summary>
/// MelonLoader-facing entry point. The implementation inherits the legacy
/// CustomCore type so existing CustomizeLib consumers keep binary compatibility.
/// </summary>
public sealed class MelonLoaderEntryPoint : global::CustomizeLib.BepInEx.CustomCore
{
}
